/**
 * Help-Seite für Hilfeinformationen
 * @module help
 */

import { bootLayout } from "../common/layout.js";

initHelpPage();

/**
 * Initialisiert die Help-Seite mit Layout-Setup
 */
async function initHelpPage() {
  await bootLayout();
}
